#Auto generated by Edalize

all: post_build

pre_build: 

design.v: edalize_yosys_template.tcl sv_file.sv tcl_file.tcl vlog_file.v vlog05_file.v vlog_incfile another_sv_file.sv | pre_build
	$(EDALIZE_LAUNCHER) yosys -l yosys.log -p 'tcl edalize_yosys_template.tcl'

design: design.v design.scr
	$(EDALIZE_LAUNCHER) iverilog -stop_module -c design.scr -o design

run: design
	$(EDALIZE_LAUNCHER) vvp -n -M. design -fst $(EXTRA_OPTIONS)

post_build: design

pre_run: 

run: pre_run

post_run: run
