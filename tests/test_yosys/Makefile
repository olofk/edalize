#Auto generated by Edalize

TARGET   := test_yosys_0

all: $(TARGET).verilog


%.blif: test_yosys_0.tcl
	yosys -l yosys.log -p "tcl $?"
%.json: test_yosys_0.tcl
	yosys -l yosys.log -p "tcl $?"
%.edif: test_yosys_0.tcl
	yosys -l yosys.log -p "tcl $?"
%.verilog: test_yosys_0.tcl
	yosys -l yosys.log -p "tcl $?"

clean:
	rm -f $(TARGET).blif $(TARGET).json $(TARGET).edif $(TARGET).v
