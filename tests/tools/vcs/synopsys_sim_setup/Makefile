#Auto generated by Edalize

all: design

work.workdir/AN.DB/make.vlogan libx.workdir/64/vhmra.sdb: sv_file.sv vhdl_lfile work.f libx.f
	$(EDALIZE_LAUNCHER) vlogan -full64 -file work.f -work work -l work.log sv_file.sv
	$(EDALIZE_LAUNCHER) vhdlan -full64 -file libx.f -work libx -l libx.log vhdl_lfile

design: work.workdir/AN.DB/make.vlogan libx.workdir/64/vhmra.sdb vcs.f parameters.txt
	$(EDALIZE_LAUNCHER) vcs -full64 -o design -file vcs.f -parameters parameters.txt

run:
	$(EDALIZE_LAUNCHER) ./design $(EXTRA_OPTIONS)
