#Auto generated by Edalize

TOP	:= test_symbiflow_nextpnr_fpga_interchange_0
PARTNAME:= xc7a35tcsg324-1csg324-1
BITSTREAM_DEVICE := artix7

all: ${TOP}.bit

${TOP}.fasm: ${TOP}-nextpnr.mk
	$(EDALIZE_LAUNCHER) $(MAKE) -f ${TOP}-nextpnr.mk

${TOP}.bit: ${TOP}.fasm
	$(EDALIZE_LAUNCHER) symbiflow_write_bitstream -d ${BITSTREAM_DEVICE} -f ${TOP}.fasm -p ${PARTNAME} -b ${TOP}.bit

clean:
	$(EDALIZE_LAUNCHER) rm -rf *.bit *.blif *.edif *.fasm *.json *.netlist *.phys *.txt *.log
