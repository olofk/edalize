#Auto generated by Edalize

NAME	:= test_vivado_nextpnr_0
PARTNAME:= xc7a35tcsg324-1csg324-1
BITSTREAM_DEVICE := artix7
BUILDDIR:= build

all: ${NAME}.bit

${BUILDDIR}:
	mkdir ${BUILDDIR}

${NAME}.fasm: ${NAME}-nextpnr.mk | ${BUILDDIR}
	$(MAKE) -f ${NAME}-nextpnr.mk

${NAME}.bit: ${NAME}.fasm
	cd ${BUILDDIR} && write_bitstream -d ${BITSTREAM_DEVICE} -f ${NAME}.fasm -p ${PARTNAME} -b ${NAME}.bit

clean:
	rm -rf ${BUILDDIR}

