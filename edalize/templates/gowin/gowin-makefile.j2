# Auto generated by Edalize
NAME := {{ name }}
OPTIONS := {{ tool_options.gowin_options|join(' ') }}
DSE_OPTIONS := {{ tool_options.dse_options|join(' ') }}

all: syn

syn: $(NAME).tcl
	$(EDALIZE_LAUNCHER) gw_sh $(OPTIONS) $(NAME).tcl

clean:
	$(EDALIZE_LAUNCHER) rm -rf *.* db incremental_db

.PHONY: all syn clean
