#Auto generated by Edalize
SHELL = bash

PROJ_DIR        := {{ proj_dir }}
TOOL            := {{ tool }}
FLAGS           := {{ flags }}
ENV_VARS        := {{ env_vars }}
VERILOG_FILES   := {{ verilog_files }}
SVERILOG_FILES  := {{ sverilog_files }}
TCL_FILES       := {{ tcl_files }}
INC_DIRS        := {{ incdirs }}
SDC	:= {{ sdc }}
PCF	:= {{ pcf }}
XDC	:= {{ xdc }}

all:
			(${ENV_VARS} ${TOOL} ${FLAGS} ${INC_DIRS} ${VERILOG_FILES} ${SVERILOG_FILES} ${TCL_FILES}) 2>&1 | tee make.log


clean:
	$(EDALIZE_LAUNCHER) rm -rf *.log *.rpt *.place *.bit *.eblif *.fasm *.json *.ioplace *.net *.route

