#Auto generated by Edalize

NAME	:= {{ top }}
PARTNAME:= {{ partname }}
BITSTREAM_DEVICE := {{ bitstream_device }}
BUILDDIR:= {{ builddir }}

all: ${NAME}.bit

${BUILDDIR}:
	mkdir ${BUILDDIR}

${NAME}.fasm: ${NAME}-nextpnr.mk | ${BUILDDIR}
	$(MAKE) -f ${NAME}-nextpnr.mk

${NAME}.bit: ${NAME}.fasm
	cd ${BUILDDIR} && write_bitstream -d ${BITSTREAM_DEVICE} -f ${NAME}.fasm -p ${PARTNAME} -b ${NAME}.bit

clean:
	rm -rf ${BUILDDIR}

