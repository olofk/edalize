#Auto generated by Edalize

all: config.tcl
ifeq ($(PDK_ROOT),)
	echo "Please export PDK_ROOT"
	exit 1
endif

	$(EDALIZE_LAUNCHER) flow.tcl -tag {{top}} -save -save_path . -design .

clean:
	rm -rf runs
