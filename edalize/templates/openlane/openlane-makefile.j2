#Auto generated by Edalize

OPENLANE_COMMAND = "cd /project/{{work_root}} && flow.tcl -design ."

all: config.tcl
ifeq ($(OPENLANE_ROOT),)
	echo "Please export OPENLANE_ROOT"
	exit 1
endif
ifeq ($(PDK_ROOT),)
	echo "Please export PDK_ROOT"
	exit 1
endif
	$(EDALIZE_LAUNCHER) $(OPENLANE_COMMAND)

clean:
	rm -rf runs