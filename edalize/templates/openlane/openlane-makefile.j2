#Auto generated by Edalize

all: config.tcl
ifeq ($(OPENLANE_ROOT),)
	echo "Please export OPENLANE_ROOT"
	exit 1
endif
ifeq ($(PDK_ROOT),)
	echo "Please export PDK_ROOT"
	exit 1
endif
	$(EDALIZE_LAUNCHER) flow.tcl -design .

clean:
	rm -rf runs
