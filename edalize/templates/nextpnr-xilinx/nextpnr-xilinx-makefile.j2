#Auto generated by Edalize

TARGET   := {{ name }}

all: $(TARGET).{{ default_target }}

{{ name }}.json: {{ name }}.mk
	$(MAKE) -f $<

{{ name }}.fasm: {{ name }}.json
	nextpnr-{{ arch }} --chipdb {{ chipdb }} --xdc {{ constr }} --json {{ name }}.json --write {{ name }}.routed.json --fasm {{ name }}.fasm

clean:
	rm -f $(TARGET).fasm $(TARGET).routed.json
