#Auto generated by Edalize

TARGET   := {{ name }}

all: $(TARGET).fasm

{{ name }}.json: {{ name }}.mk
	$(MAKE) -f $<

{{ name }}.fasm: {{ name }}.json
	nextpnr-xilinx --chipdb {{ chipdb }} --xdc {{ constr }} --json {{ name }}.json --write {{ name }}.routed.json --fasm {{ name }}.fasm --log nextpnr_xilinx.log

clean:
	rm -f $(TARGET).fasm $(TARGET).routed.json
